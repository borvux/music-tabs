<nav class="navbar navbar-expand-md bg-body-tertiary sticky-top bg-light">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand fs-2 text-decoration-none text-dark ms-2" do %>
      <i class="fa-solid fa-music fs-1 text-info"></i>
      Music Tabs
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 home-tabs-center">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Home", root_path, class: "nav-link", aria: { current: "page" } %>
          </li>
          <li class="nav-item">
            <%= link_to "Tabs", tabs_path, class: "nav-link", aria: { current: "page" } %>
          </li>
        <% end %>
      </ul>
      <% if user_signed_in? %>
        <div class="dropdown d-none d-md-block ms-auto mb-2 mb-md-0">
          <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fa-solid fa-bars"></i>
          </button>
          <ul class="dropdown-menu dropdown-menu-end">
            <%= link_to edit_user_registration_path, class: "dropdown-item text-secondary mb-2 mb-md-0", aria: { current: "page" } do %>
              Settings <i class="bi bi-gear-fill"></i>
            <% end %>
            <%= link_to "#", class: "dropdown-item text-danger", data: { bs_toggle: "modal", bs_target: "#logoutConfirmationModal", path: destroy_user_session_path } do %>
              Logout <i class="fa-solid fa-right-from-bracket"></i>
            <% end %>
          </ul>
        </div>
        <ul class="navbar-nav d-md-none ms-auto mb-2 mb-md-0 p-0">
          <li class="nav-item">
            <%= link_to edit_user_registration_path, class: "nav-link text-secondary", aria: { current: "page" } do %>
              Settings <i class="bi bi-gear-fill"></i>
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to "#", class: "nav-link text-danger", data: { bs_toggle: "modal", bs_target: "#logoutConfirmationModal", path: destroy_user_session_path } do %>
              Logout <i class="fa-solid fa-right-from-bracket"></i>
            <% end %>
          </li>
        </ul>
      <% else %>
        <ul class="navbar-nav ms-auto mb-2 mb-md-0 p-0">
          <li class="nav-item d-flex align-items-center">
            <%= link_to "Sign-in", new_user_session_path, class: "btn btn-sm btn-outline-secondary", aria: { current: "page" } %>
          </li>
          <li class="nav-item d-flex align-items-center">
            <%= link_to "Sign-up", new_user_registration_path, class: "btn btn-info mb-2 mb-md-0", aria: { current: "page" } %>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>

<%= render 'shared/modal', title: 'Confirm Logout', body: 'Are you sure you want to logout?', yes: 'Logout', no: 'Cancel', yes_btn: 'btn btn-danger', no_btn: 'btn btn-info', devise_action: true, modal_id: 'logoutConfirmationModal' %>

<%= javascript_include_tag 'link_modal' %>
