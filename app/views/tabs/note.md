Might be a good idea to move the landing page/welcome stuff into a different views 
and then have a separate controller (e.g. `home_controller.rb`) for them. 
As it stands, the `views/tabs` folder is a little big.
